service.js와 store.js 업데이트를 위한 생성 프롬프트:

1. 공통 파라미터 주석 추가:
   - 모든 service.js 및 store.js 최상단에 필터링/정렬 파라미터 사용법 주석 추가
     "// Common usage: pass a `params` object to list methods for filtering and ordering, e.g. { status: 'active', ordering: '-id' }"

2. async/await 및 try/catch 래핑:
   - 모든 service 메서드를 async/await로 변경하고, 오류 시 showToast 호출
   - 성공 시에도 showToast 호출하기

3. domUtils.showToast import 및 호출:
   - service.js 상단에 import { showToast } from './domUtils'; 추가
   - HTTP 요청 성공/실패 시 showToast(type="success" 또는 "error") 호출

4. store.js 한국어 사용법 주석 추가:
   - 최상단에 사용방법 주석: 컴포넌트 import, setup 초기화, 액션 호출, state 참조 예시

5. ViewSet 필터·정렬 파라미터 전달 지원:
   - listXXX(params) 메서드에 params 인자 사용

6. 추가 기능:
   - by-variety, dict/list endpoints 지원 메서드 포함

7. 다중 파라미터 지원 및 django-filter 유틸 사용 (추가):
   - 클라이언트에서 동일 키를 여러번 전송하는 다중 파라미터(key=value&key=value) 지원
   - src/utils/django-filter.js에 buildDjangoFilterParams 구현을 사용하여 params를 URLSearchParams로 변환
   - 서비스 레이어에서 buildDjangoFilterParams(params)를 사용하도록 수정
   - 서버(DRF)에서는 request.GET.getlist() 또는 MultipleChoiceFilter로 동일 키 반복 파라미터를 처리해야 함

파일 경로:
- services/agriseed.service.js
- services/corecode.service.js
- services/lsissocket.service.js
- services/agriseed.store.js
- services/corecode.store.js
- services/lsissocket.store.js

위 항목을 반영해 각각 파일을 업데이트해주세요.